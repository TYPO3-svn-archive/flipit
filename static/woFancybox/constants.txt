plugin.tx_flipit {
  configuration {
      // Don't use fancybox
    enableFancybox = disabled
  }
  html {
      // Size of HTML content in case of no fancybox
    height  = 630px
    width   = 890px
  }
}